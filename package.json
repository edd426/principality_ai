{
  "name": "principality_ai",
  "version": "1.0.0",
  "description": "a Dominion inspired card game with MCP support",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces",
    "test:coverage": "npm run test:coverage --workspaces",
    "lint": "npm run lint --workspaces",
    "build": "npm run build --workspaces",
    "play": "npm run build && node packages/cli/dist/index.js",
    "test-engine": "node -e \"const {GameEngine} = require('./packages/core/dist/game.js'); console.log('✓ Loading engine...'); const engine = new GameEngine('12345'); const gameState = engine.initializeGame(1); console.log('✓ Game initialized'); console.log('Hand:', gameState.players[0].hand.join(', ')); console.log('Phase:', gameState.phase); console.log('🎮 Engine working!');\n\"",
    "check-traceability": "npx ts-node scripts/check-traceability.ts",
    "check-traceability:fix": "echo '🔧 Manual fixes needed - see traceability-report.json for details'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/edd426/principality_ai.git"
  },
  "keywords": [
    "dominion",
    "card-game",
    "mcp",
    "ai",
    "typescript"
  ],
  "author": "Evan DeLord",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/edd426/principality_ai/issues"
  },
  "homepage": "https://github.com/edd426/principality_ai#readme",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^8.44.0",
    "@typescript-eslint/parser": "^8.44.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
